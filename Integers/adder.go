package integers

// When you have more than one argument of the same type
// you can shorten the declaration to have the type once
// after the last parameter
func Add(x, y int) (result int) {
	result = x + y
	return
}
